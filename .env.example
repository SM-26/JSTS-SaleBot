BOT_TOKEN=your_telegram_bot_token_here
MONGO_URI=mongodb://localhost:27017/SalesBotDB

# Optional. Connection attempts before the bot gives up and exits (default 5,
# 5 seconds apart). Docker restarts it, so it retries until Mongo is reachable.
MONGO_MAX_ATTEMPTS=5
